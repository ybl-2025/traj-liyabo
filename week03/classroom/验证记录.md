# 课堂包验证记录

日期：2026-09-23。

环境：Windows x64，Python 3.9.13，PyTorch 1.12.1 CPU，NumPy 1.26.4，SciPy 1.13.1，两线程。测试复用了本机已安装的基础依赖，缺少的依赖安装在独立测试目录；尚未在学生的全新电脑上验证完整安装过程。

## 已完成

- 下载并核对固定版本的 75 个上游文件，只有根包初始化文件做了一处可选经典模型导入调整。
- 原始课程数据逐字节复制一致，训练、验证、测试帧集合互不重叠。
- 使用 TrajNet++ Reader 读取全部转换场景，每个场景形状为 20 × 64 × 2，无缺失坐标。
- 原版 Social LSTM 完成 1 轮训练及模型重载评价。
- 课堂默认小模型完成 10 轮训练、逐轮上游验证和保存，总耗时 63.53 秒。该耗时不含 Python 启动与首次依赖安装，不能保证其他电脑或其他池化结构同速。
- 历史输入递推评价完成，验证集预测约 0.76 秒，测试集预测约 0.89 秒，不含加载与绘图时间。脚本输出误差 JSON 和对比图。

## 本次固定 10 轮模型的结果

| 集合 | 模型 ADE | 模型 FDE | 匀速 ADE | 匀速 FDE |
|---|---:|---:|---:|---:|
| 验证 | 0.5672 | 1.0947 | 0.2373 | 0.4491 |
| 测试 | 0.7401 | 1.4295 | 0.1251 | 0.2339 |

以上使用换算后坐标单位，米制仍依赖待确认的原始厘米单位假设。每个集合评价 12 个场景的主要行人，所有邻居从历史递推，未用真实未来输入模型。模型没有超过匀速基线；这是小样本、单次实验的运行检查结果，不能作为完整论文复现或机制优劣结论。

包内不分发本次测试产生的权重或机器路径日志，学生自行运行即可生成。
